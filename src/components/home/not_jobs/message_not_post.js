import React from 'react';
import * as Styles from './styles';

const not_jobs = () => {
  return (
      <Styles.Container>
          <Styles.MessageNotPost>
          we still don't have jobs in that area! :/
          </Styles.MessageNotPost>
      </Styles.Container>
  )
}

export default not_jobs;